/**
 * PDF Import placeholder.
 * Phase 3:
 * Upload PDF → Extract questions → Save DRAFT_QUESTIONS → Admin review → Publish.
 */

const PdfImport = (() => {
  function status() {
    return "PDF import placeholder ready for Phase 3.";
  }
  return { status };
})();
